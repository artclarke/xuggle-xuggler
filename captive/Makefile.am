include $(abs_top_builddir)/mk/Makefile.global
if CAPTIVE_ENABLE_LIBMP3LAME
  LIBMP3LAME=libmp3lame
endif
if CAPTIVE_ENABLE_LIBSPEEX
  LIBSPEEX=libspeex
endif
if CAPTIVE_ENABLE_FFMPEG
  FFMPEG=ffmpeg
endif
SUBDIRS= \
  $(LIBMP3LAME) $(LIBSPEEX) $(FFMPEG)


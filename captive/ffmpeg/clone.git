#!/bin/sh

# This script is meant to be run after you clone IF you want to be
# a developer and integrate from upstream projects.  It's purpose
# is to add all remote origins; each captive library will
# have its own pull.git script which will be used to pull the
# branch that the current version is tied to.
git remote add -f ffmpeg-remote git://source.ffmpeg.org/ffmpeg.git 2>/dev/null
git remote add -f libav-remote git://git.libav.org/libav.git 2>/dev/null

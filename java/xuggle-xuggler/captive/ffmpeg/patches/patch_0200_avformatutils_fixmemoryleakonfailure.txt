Index: libavformat/utils.c
===================================================================
--- libavformat/utils.c	(revision 19327)
+++ libavformat/utils.c	(working copy)
@@ -499,6 +499,7 @@
     av_freep(&pd->buf);
     if (pb)
         url_fclose(pb);
+    av_free(*ic_ptr);
     *ic_ptr = NULL;
     return err;
 
